// Deprecated: kept as a no-op stub during the .ts -> .js migration.
// The real config lives in next.config.js. Safe to delete after clean install.
// Using `export default` here would re-introduce the original "TS config unsupported" error
// for any tooling that still loads this file, so the body is intentionally empty.
export {};
